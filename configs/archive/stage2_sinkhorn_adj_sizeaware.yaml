# GraphQMap Stage 2: Sinkhorn + size-aware L_adj (Phase C, Round 2)
#
# Inherits structure from stage2_sinkhorn_adj.yaml but replaces the constant
# adjacency component with adjacency_size_aware. The piecewise weights are
# calibrated to the C1 finding that L_adj=1.0 helps Torino (133Q) but hurts
# Toronto (27Q):
#   small  (h <= 40):       0.3   — Toronto and all training backends ≤ 33Q
#   medium (40 < h <= 80):  0.5   — Brooklyn (65Q), training 53/65Q
#   large  (h > 80):        1.0   — Torino (133Q), training 127Q × 9
#
# All other settings identical to stage2_sinkhorn_adj.yaml; hw_feat_v1
# (node 5dim, edge 1dim) is enforced to match the validated baseline.

model:
  embedding_dim: 64

  circuit_gnn:
    node_features:
      - gate_count
      - two_qubit_gate_count
      - single_qubit_gate_ratio
      - critical_path_fraction
    rwpe_k: 2
    edge_input_dim: 3
    num_layers: 3
    num_heads: 4
    activation: elu
    dropout: 0.1
    use_batchnorm: true
    residual_layers: [2, 3]

  hardware_gnn:
    node_input_dim: 5            # hw_feat_v1: readout, sq_err, degree, t1_cx_ratio, t2_cx_ratio
    edge_input_dim: 1            # hw_feat_v1: 2q_error
    num_layers: 3
    num_heads: 4
    activation: elu
    dropout: 0.1
    use_batchnorm: true
    residual_layers: [2, 3]

  cross_attention:
    num_layers: 2
    num_heads: 4
    ffn_hidden_dim: 128
    dropout: 0.1

  score_head:
    d_k: 64
    noise_bias_dim: 0

sinkhorn:
  score_norm: sinkhorn
  tau_max: 1.0
  tau_min: 0.05
  schedule: exponential
  max_iter: 20
  tolerance: 1.0e-6

training:
  optimizer:
    type: adamw
    lr: 5.0e-4
    weight_decay: 1.0e-4
  scheduler:
    type: cosine_annealing
    T_max: 100
    eta_min: 1.0e-5
  warmup_epochs: 2
  grad_clip_norm: 2.0
  max_epochs: 100
  early_stopping:
    monitor: val_pst
    patience: 10
    min_delta: 0.005
  pst_validation:
    interval: 5
    num_circuits: 200
    convergence_checks: 3

loss:
  type: surrogate
  components:
    - name: error_distance
      weight: 1.0
    - name: adjacency_size_aware
      weight: 1.0                # outer multiplier; effective weights set in params
      params:
        weight_small: 0.3
        weight_medium: 0.5
        weight_large: 1.0
        threshold_small: 40
        threshold_large: 80

data:
  data_root: data/circuits
  splits:
    train: data/circuits/splits/stage2_all.json
    val: data/circuits/splits/val.json
  multi_programming:
    scenarios: [1, 2, 4]
    proportions: [0.5, 0.3, 0.2]
    occupancy_min: 0.3
    occupancy_max: 0.75
  normalization:
    epsilon: 1.0e-8

batching:
  max_total_nodes: 512
  backend_balanced: true
  large_backend_boost: 2.0

backends:
  training:
    - FakeAthens
    - FakeBelem
    - FakeBogota
    - FakeBurlington
    - FakeEssex
    - FakeLima
    - FakeLondon
    - FakeManila
    - FakeOurense
    - FakeQuito
    - FakeRome
    - FakeSantiago
    - FakeValencia
    - FakeVigo
    - FakeYorktown
    - FakeCasablanca
    - FakeJakarta
    - FakeLagos
    - FakeNairobi
    - FakeOslo
    - FakePerth
    - FakeMelbourne
    - FakeGuadalupe
    - FakeAlmaden
    - FakeBoeblingen
    - FakeJohannesburg
    - FakePoughkeepsie
    - FakeSingapore
    - FakeAlgiers
    - FakeAuckland
    - FakeCairo
    - FakeCambridge
    - FakeGeneva
    - FakeHanoi
    - FakeKolkata
    - FakeMontreal
    - FakeParis
    - FakePeekskill
    - FakeSydney
    - FakePrague
    - FakeBrooklyn
    - FakeBrisbane
    - FakeCusco
    - FakeKawasaki
    - FakeKyiv
    - FakeKyoto
    - FakeOsaka
    - FakeQuebec
    - FakeSherbrooke
    - FakeTorino
  validation:
    - FakeMumbai             # 27Q
    - FakeManhattan          # 65Q
  test:
    - FakeToronto
    - FakeRochester
    - FakeWashington

seed: 42
device: cuda
num_workers: 4
log_dir: logs/stage2
checkpoint_dir: checkpoints/stage2
