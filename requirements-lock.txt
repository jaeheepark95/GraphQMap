# GraphQMap Pinned Dependencies (verified on CUDA 12.1/12.2)
# Install guide:
#   1. pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   2. pip install -r requirements-lock.txt
# Note: PyTorch must be installed first with the correct CUDA index URL.
#       NVIDIA sub-packages (nvidia-cublas-cu12, etc.) are managed by torch — do NOT pin them here.

# Core ML (torch installed separately — see above)
torch-geometric==2.6.1
numpy==1.23.5

# Quantum
qiskit==1.2.4
qiskit-ibm-runtime==0.29.1
qiskit-aer-gpu==0.15.1
cuquantum-cu12==24.8.0
custatevec-cu12==1.6.0.post1
cutensornet-cu12==2.5.0
rustworkx==0.15.1

# Data & computation
scipy==1.14.1
pandas==2.3.3
PyYAML==6.0.3
networkx==3.3
tabulate==0.10.0  # required by pandas.DataFrame.to_markdown() in evaluate.py

# Visualization
matplotlib==3.9.2

# Testing
pytest==8.4.2
